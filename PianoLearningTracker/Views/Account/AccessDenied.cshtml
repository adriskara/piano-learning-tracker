@{
    ViewData["Title"] = "Pristup odbijen";
}

<div class="auth-card">
    <div class="auth-icon">🚫</div>
    <h2 class="auth-title">Pristup odbijen</h2>
    <p class="auth-message">Nemate dovoljno prava za pristup ovoj stranici.</p>
    <a asp-controller="Home" asp-action="Index" class="btn btn-primary">Natrag na početnu</a>
</div>
