@model IEnumerable<PianoLearningTracker.Models.Teacher>

@{
    ViewData["Title"] = "Profesori";
}

<ul class="breadcrumb-list">
    <li><a asp-controller="Home" asp-action="Index">Dashboard</a></li>
    <li>Profesori</li>
</ul>

<div class="page-toolbar">
    <div class="search-bar">
        <span class="search-icon">🔍</span>
        <input type="text" id="search-input" class="search-input" placeholder="Pretraži profesore..." autocomplete="off" />
    </div>
    @if (User.IsInRole("Administrator"))
    {
        <a class="btn btn-primary" asp-action="Create">+ Novi profesor</a>
    }
</div>

<div id="teachers-container">
    @await Html.PartialAsync("_TeacherList", Model)
</div>

@section Scripts {
    <script>
        // AJAX pretraga profesora
        var searchTimer;
        document.getElementById('search-input').addEventListener('input', function () {
            clearTimeout(searchTimer);
            var q = this.value;
            searchTimer = setTimeout(function () {
                fetch('/nastavnici/pretraga?q=' + encodeURIComponent(q), {
                    headers: { 'X-Requested-With': 'XMLHttpRequest' }
                })
                .then(function (r) { return r.text(); })
                .then(function (html) {
                    document.getElementById('teachers-container').innerHTML = html;
                    animateCards();
                });
            }, 300);
        });

        animateCards();
    </script>
}
