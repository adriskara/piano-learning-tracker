@model IEnumerable<PianoLearningTracker.Models.Piece>

@{
    ViewData["Title"] = "Skladbe";
}

<ul class="breadcrumb-list">
    <li><a asp-controller="Home" asp-action="Index">Dashboard</a></li>
    <li>Skladbe</li>
</ul>

<div class="page-toolbar">
    <div class="search-bar">
        <span class="search-icon">🔍</span>
        <input type="text" id="search-input" class="search-input" placeholder="Pretraži skladbe ili skladatelje..." autocomplete="off" />
    </div>
    @if (User.IsInRole("Administrator"))
    {
        <a class="btn btn-primary" asp-action="Create">+ Nova skladba</a>
    }
</div>

<div id="pieces-container">
    @await Html.PartialAsync("_PieceList", Model)
</div>

@section Scripts {
    <script>
        // AJAX pretraga skladbi
        var searchTimer;
        document.getElementById('search-input').addEventListener('input', function () {
            clearTimeout(searchTimer);
            var q = this.value;
            searchTimer = setTimeout(function () {
                fetch('/skladbe/pretraga?q=' + encodeURIComponent(q), {
                    headers: { 'X-Requested-With': 'XMLHttpRequest' }
                })
                .then(function (r) { return r.text(); })
                .then(function (html) {
                    document.getElementById('pieces-container').innerHTML = html;
                    animateCards();
                });
            }, 300);
        });

        animateCards();
    </script>
}
