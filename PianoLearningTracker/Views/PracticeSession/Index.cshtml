@model IEnumerable<PianoLearningTracker.Models.PracticeSession>

@{
    ViewData["Title"] = "Practice Sessions";
}

<ul class="breadcrumb-list">
    <li>Dashboard</li>
    <li>Practice Sessions</li>
</ul>

<div class="entity-grid">
    @foreach (var session in Model)
    {
        <article class="entity-card">
            <h2 class="entity-card-title">Session #@session.Id</h2>
            <p class="entity-card-meta">@session.Date:dddd, MMM d yyyy</p>
            <p class="entity-card-meta">Student: @session.Student.FirstName @session.Student.LastName</p>
            <p class="entity-card-meta">Piece: @session.Piece.Title</p>
            <div class="entity-card-actions mt-3">
                <a class="btn btn-primary btn-sm" asp-action="Details" asp-route-id="@session.Id">View details</a>
            </div>
        </article>
    }
</div>
